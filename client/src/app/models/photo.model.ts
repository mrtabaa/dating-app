export interface Photo {
    schema: string;
    url165: string; // navbar & thumbnail
    url256: string; // card
    urlEnlarged: string; // enlarged photo up to ~300kb
    isMain: boolean;
}
