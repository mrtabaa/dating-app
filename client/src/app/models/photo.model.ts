export interface Photo {
    schema: string;
    url_165: string; // navbar & thumbnail
    url_256: string; // card
    url_enlarged: string; // enlarged photo up to ~300kb
    isMain: boolean;
}
