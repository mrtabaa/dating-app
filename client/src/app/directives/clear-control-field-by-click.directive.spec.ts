import { ClearControlFieldByClick } from './clear-control-field-by-click.directive';

describe('dirClearControlField', () => {
  it('should create an instance', () => {
    const directive = new ClearControlFieldByClick();
    expect(directive).toBeTruthy();
  });
});
