import { CdkDynamicHeightDirective } from './cdk-dynamic-height.directive';

describe('CdkDynamicHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new CdkDynamicHeightDirective();
    expect(directive).toBeTruthy();
  });
});
