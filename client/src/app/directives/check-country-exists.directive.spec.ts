import { CheckCountryExistsDirective } from './check-country-exists.directive';

describe('CheckCountryExistsDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckCountryExistsDirective();
    expect(directive).toBeTruthy();
  });
});
